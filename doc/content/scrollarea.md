# ScrollArea

Scrollable container (both X and Y enabled by default).

## Create

```go
sa := ui.NewScrollArea()
sa.AddWidgetOnGrid(bigWidget, 0, 0)
```

The widget shows scrollbars when content exceeds viewport size.

